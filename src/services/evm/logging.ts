export { createLogger as createEvmLogger } from "@shared/logger";
export type { Logger as EvmLogger } from "@shared/logger";
export { type StructuredLogEntry, type LogLevel, readRecentLogs, getLogFilePath } from "@shared/logger";
